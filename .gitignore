*.db
.DS_Store
__pycache__/
*.pyc
*.pyo
.env
venv/
.venv/
.venv*/
*.egg-info/
dist/

# Discovery pipeline — local output only (logs, reports, metadata CSVs), never committed
discovery/output/

# Portal-uploaded binaries — live on server alongside transactions.db, never committed
data/uploads/
