# Processing pipeline dependencies — local use only, not installed on the server.
# Install with: pip install -r processing/requirements.txt
pyyaml>=6.0
pandas>=2.0
openpyxl>=3.1
sqlalchemy>=2.0

# OCR pipeline
pdf2image>=1.16
Pillow>=10.0
pytesseract>=0.3
rapidfuzz>=3.0
scikit-image>=0.21
numpy>=1.24
requests>=2.28
# paddlepaddle / paddleocr — optional secondary engine, no Python 3.14 wheel yet
