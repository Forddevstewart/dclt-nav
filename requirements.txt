# Server dependencies only — deployed to VPS.
# Discovery/scraping deps live in discovery/requirements.txt (local use only).
flask>=3.0
flask-login>=0.6
gunicorn>=22.0
pyyaml>=6.0
pandas>=2.0
sqlalchemy>=2.0
openpyxl>=3.1
